{
  "name": "mcp-mqtt-plc-server",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for MQTT-PLC communication",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc && node dist/server.js",
    "watch": "tsc --watch",
    "test-mqtt": "tsc && node test-mqtt.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "dotenv": "^17.2.1",
    "mqtt": "^5.14.0"
  },
  "devDependencies": {
    "@types/mqtt": "^0.0.34",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
